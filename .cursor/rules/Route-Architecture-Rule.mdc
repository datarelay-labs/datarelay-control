---
alwaysApply: true
---

Never introduce duplicated Streams.

Destination-specific behavior must always be implemented using Routes.

One Stream
↓
Many Routes
↓
Many Destinations

Route Processing (Transform → Protection → Classification → Policy → Delivery)
is the only supported product runtime path.

Never resurrect legacy stream-scoped Route Processing OFF / parallel fan-out as a
first-class runtime.
