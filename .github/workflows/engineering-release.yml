name: Engineering Release Contract

on:
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Exact candidate SHA
        required: true
        type: string
      phase:
        description: Release contract phase
        required: true
        type: choice
        default: qualify
        options:
          - qualify
          - post-release

permissions:
  contents: read

jobs:
  release-contract:
    uses: datarelay-labs/engineering-system/.github/workflows/release-contract.yml@53d4d54f9c33667a9cd89855b4d5f460825b0610
    with:
      expected_sha: ${{ inputs.expected_sha }}
      phase: ${{ inputs.phase }}
      profile_path: .engineering/release.yaml
