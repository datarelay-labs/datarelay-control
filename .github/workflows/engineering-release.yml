name: Engineering Release Contract

on:
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Exact candidate SHA
        required: true
        type: string
      phase:
        description: Release contract phase
        required: true
        type: choice
        default: qualify
        options:
          - qualify
          - post-release

permissions:
  contents: read

jobs:
  release-contract:
    uses: datarelay-labs/engineering-system/.github/workflows/release-contract.yml@fe0c05d0508564512b3eaae96e35a84699d4c728
    with:
      expected_sha: ${{ inputs.expected_sha }}
      phase: ${{ inputs.phase }}
      profile_path: .engineering/release.yaml
